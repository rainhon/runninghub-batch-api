# RunningHub API 配置
# 建议为 App 任务和 API 任务配置不同的 API Key，以实现更好的隔离

# App 任务专用 API Key（用于调用 AI 应用）
# 适用场景: 通过 AI 应用的 webappId 调用（runninghub.py）
RUNNINGHUB_APP_TASK_KEY=your_app_task_api_key_here

# 直接 API 任务专用 API Key（用于直接 API 调用）
# 适用场景: 直接调用文生图、图生图、文生视频等 API（api_client_wrapper.py）
RUNNINGHUB_DIRECT_API_KEY=your_direct_api_task_key_here

# 兼容旧配置（如果上面的新配置未设置，将回退使用这个）
RUNNINGHUB_API_KEY=your_api_key_here

# 是否使用模拟服务（用于测试和开发）
# 设置为 true 时，不会调用真实 API，而是使用模拟数据
# - App 任务: 使用 mock_runninghub.py
# - API 任务: 使用 mock_api_client.py
USE_MOCK_SERVICE=false

